{
  "name": "pullstok-erp",
  "private": true,
  "version": "1.0.0",
  "description": "Pullstok ERP - sistema de stock multi-tenant",
  "scripts": {
    "dev:api": "pnpm --filter ./api dev",
    "dev:front": "pnpm --filter ./pullstok-front dev",
    "dev:landing": "pnpm --filter ./pullstok-landing dev",
    "dev:tienda": "pnpm --filter ./pullstok-tienda dev",
    "build": "pnpm -r build"
  },
  "engines": {
    "node": ">=20"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "prisma",
      "esbuild"
    ]
  }
}
